# Backend API (include /api/v1). Leave unset in local dev to use Next.js proxy (/api/v1).
# NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1

# Used by next.config.ts rewrites when the frontend runs in Docker or custom setups
# BACKEND_URL=http://localhost:8000
